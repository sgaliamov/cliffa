# CLI

Mini CLI framework.

1. Parses the command line arguments.
1. Loads the configuration file and environment variables.
1. Handles CTRL-C and termination signals.

To run the sample application:

``` sh
cargo run --example main
```

## To do

- show progress lib.
